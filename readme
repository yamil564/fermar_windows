Errores:
#MySQL Error: Expression #2 of SELECT list is not in GROUP BY clause and contains nonaggregated column 'kndpe_sgp.PRI.pri_in11_cod' which is not functionally dependent on columns in GROUP BY clause; this is incompatible with sql_mode=only_full_group_by
SET GLOBAL sql_mode=(SELECT REPLACE(@@sql_mode,'ONLY_FULL_GROUP_BY',''));

